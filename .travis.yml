dist: xenial # required for Python >= 3.7
language: python
python:
  - "3.8"
before_install:
  - pip install poetry
install:
  - poetry install -v
script:
  - poetry run pylint ninjavis.py
  - poetry run mypy ninjavis.py
